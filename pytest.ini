[pytest]

log_file= test.log
log_file_level= DEBUG
log_file_format= %(asctime)s - %(levelname)s - %(message)s

marker=
    system: marker to the system test 
    unit: use to test components 
    wip: work in progress 